# Автономный дневной прогон — 20.07.2026. Ты работаешь один ~2 часа, без человека.

**Цель:** непрерывно двигать корпус к состоянию «структурно верный + текст либо чист, либо
помечен», не останавливаясь. Человек вернётся через ~2 ч и откроет отчёт. Модели ПОДНЯТЫ
(день) — VLM/LLM-арбитр доступен.

## ГЛАВНЫЙ ПРИНЦИП (из AUTONOMY.md / NIGHT_RUN.md)
Неуверенность — НЕ повод останавливаться. **Неуверенность = пометить (`needs_review` +
`confidence` + карантин) и идти дальше.** Чинишь всё, что можешь уверенно; спорное — в очередь
и ДАЛЬШЕ. НЕ спрашивай человека — делай разумный выбор, фиксируй допущение в отчёте, продолжай.
PASS может падать — это норма fail-closed (I11). Критерий приёмки НЕ «PASS не упал», а: контр.
группа I1 цела, `structured_coverage` растёт, дельта объяснима по причинам.

## ПОЗИЦИЯ НА СТАРТ (по git ветки audit-fix-2026-07)
- Фаза 0 (хвост fail-closed) — сделано.
- Baseline refresh (Jul-20) — сделано: report.json 522/140/60, дрейф объяснён.
- Фаза 1 content-start: ШАГ 2 (START_IN_TOC) — сделано (КР715_2 -> OK). **ОСТАЛОСЬ: ШАГ 3
  (front-matter), ШАГ 4 (numbering model), ШАГ 5 (каноничность заголовка).**
- Фаза 2 (латиница авто-доля + self-repair) — НЕ начата.

## ИНСТРУМЕНТ: скринер порчи как recall-детектор + гейт регрессии
`_corpus/corruption_screen.py` (готов, untracked). Даёт PRECISE-сигнатуры (actionable):
`adjacent_mixed` (~4844), `digit_in_cyrword` (~2600, ATC-коды с кир.буквой), `shattered_latin`
(~2750, рассыпанные лат.слова типа `virus`->`у 1 ш з`), `impossible_start` (~696, `ЫК`->`NK`),
`homoglyph_in_latin` (~174). 602/701 док задето. Флаги: `_corpus/screen_flags.jsonl`
(спан-уровень, поле bucket=precise). OOV-ведро за `--oov` (шумно, 253k — НЕ использовать в авто).
- **Использование:** флаги precise питают резолвер (self-repair -> OCR -> арбитр -> карантин).
- **Регрессия:** `python _corpus/corruption_screen.py outout_latin --baseline <prev.json>` —
  после каждого фикса латиницы число precise-хитов ОБЯЗАНО падать; рост = регресс, разбирать.
- Скринер ловит форму. Чистые гомографы, похожие на рус.слова (`Сапсег`/`Сгоир`), он НЕ видит —
  их берут только self-repair (внутридок.) и арбитр. Это ЗНАЕМ, не «дыра».

## ОЧЕРЕДЬ ЗАДАЧ (приоритетный порядок; каждая — отдельный коммит, свой замер регресса + I1)

### A. Дочистить Фазу 1 (структура — без моделей). Промпт 12, шаги 3-5.
A1. **ШАГ 3 START_IN_FRONTMATTER** — front matter (сокращения/термины/состав РГ/конфликт
    интересов) в RegionPolicy, исключить из зоны content-start; РАЗДЕЛИТЬ склейку
    `excluded.toc[0]` (оглавление+сокращения+термины). Эталоны КР891_1, КР115_2, КР467_3.
A2. **ШАГ 4 NUMBERING_MISMATCH** — в NumberingPolicy перебор моделей нумерации, выбор по
    каноническому recall; <5/7 -> честный карантин. Снять частные ограничения (главы 1-7,
    «арабский=level1», отключение римских). Эталоны КР284_2, КР750_1.
A3. **ШАГ 5 каноничность** — убрать `startswith` в `_valid_main_title` -> полное норм.
    совпадение; промоушен по слабым сигналам ТОЛЬКО при совпадении с оглавлением. Трогает 722 ->
    `manifest --verify`, дельта <= ~30, КР848_1 остаётся не-PASS, КР66_4/КР876_1 остаются PASS.

### B. Фаза 2 — латиница: авто-доля вверх, очередь вниз (это цель «минимум человека»).
B1. **Пословный кроп (I30 #4)** — eng-OCR по кропу ЦЕЛЕВОГО слова (не строки), честно
    перемерить авто-долю (факт, не оценка).
B2. **Локализация кодов (I30 #5)** — код из выровненной позиции eng-слова; дозовые единицы
    (мг/мл/ЕД/МЕ) вне код-детекции.
B3. **Задача 2.5 (битые слова, что токен-детектор пропускает):**
    a) **intra-doc self-repair** — если якорное слово битое, а в ЭТОМ ЖЕ документе есть чистая
       форма фразы, чинить по образцу. Провенанс `intra_doc_selfrepair`. Детерминизм, без OCR.
    b) **run-level детектор** (используй `shattered_latin` из скринера) -> eng-OCR по кропу
       ВСЕЙ строки/фразы (широкий, т.к. границы слова разрушены — противоположно B1).
    c) **гомограф в латинице** — снять freq-защиту с одиночной кир-буквы в лат.окружении.
    Фикстура (в тесты, НЕ пин): КР1_4 §2.3 `Hepatitis C virus` чист во ВСЕХ 5 вхождениях;
    `virus` без `у 1 ш з`; `С`/`В` меж латиницей -> Latin C/B.
B4. **2.3 VLM/LLM-арбитр (МОДЕЛИ ПОДНЯТЫ)** — запускать ТОЛЬКО по precise-флагам скринера +
    маленькая выборка «чистого» текста для оценки доли пропусков. Под AND-гейтом I28/I29
    (гомоглифы из `_CYR2LAT`, тест по всему токену, подтверждение ПИКСЕЛЯМИ цели не соседом,
    стоп по стадиям). Расхождение ног -> `needs_review`. VLM НИКОГДА не молчаливый авторитет.
B5. **Пересобрать очередь** `_corpus/verify_queue/` пословными кропами; доложить
    auto/needs_review/критических; прогнать скринер --baseline -> хиты precise упали.

### ГЕЙТ FAIL-CLOSED (сквозной, обязателен)
Любой битый спан, не почищенный уверенно ни одним каналом -> `needs_review` + `confidence` +
карантин. НИКОГДА молча в текст (урок пина `рб1->PDI`). «Полностью чисто» = «ничего грязного
молча», а не «ноль пропусков».

## СТОП-УСЛОВИЯ (только три; за 11+ промптов ни одно не сработало)
- **E1 — ломаешь здоровое:** контр.группа I1 (КР802_1/845_1/901_1/1000_1/66_4/876_1) ушла из
  PASS и не чинится за 2 попытки. Стоп по этому каналу.
- **E2 — теряешь контент:** `owned_before ⊄ owned_after` (спан исчез без владельца) ИЛИ
  вычтенный текст не покрыт ничьим raw_text.
- **E3 — нужны деньги/люди/железо:** наём разметчиков, слом схемы, перезаливка коллегам.
Во ВСЕХ остальных случаях — НЕ останавливаешься. Авария в одном канале не блокирует другие:
пиши `_corpus/DAY_BLOCKED.md` и продолжай остальное.

## КОММИТЫ И BASELINE
- Малые осмысленные коммиты по-русски, без упоминания ИИ, без Co-Authored-By. Не коммитить на
  красных тестах B/C. **Тест обязан ИСПОЛНЯТЬ путь (перепарс с OCR ON), а не читать артефакт (I30).**
- `baseline_manifest.json` (подвижный) двигать ТОЛЬКО по ритуалу I7: причина в коммите +
  старый эталон сохранён датированным файлом. После output-меняющего фикса — `manifest --verify`
  + дельта по причинам.
- OCR: выставить `TESSERACT_CMD`/`TESSDATA_PREFIX` (I5), иначе eng-OCR молча не сработает.

## К ВОЗВРАЩЕНИЮ ЧЕЛОВЕКА (оставить)
`_corpus/DAY_REPORT.md`: что сделано по шагам; дельта статусов по причинам; скринер до/после
(precise-хиты); авто/needs_review/критических; какие тесты зелёные/красные и почему; что ушло
в карантин и почему; список коммитов; дописанное в INVARIANTS; честный вердикт — где затык.
Плюс обновлённая `_corpus/verify_queue/` (спорное на человека, минимизировано).

## КООРДИНАЦИЯ
Единственный исполнитель продуктового кода — ТЫ (Claude Code). Cowork-ассистент в код/git не
лезет; `_corpus/corruption_screen.py` — его готовый инструмент, бери и пользуйся.
