# Очередь готова к разметке (Label Studio) — промпт 17, ШАГ 6

Дата: 2026-07-20. Корпус `outout_latin` перегенерирован текущим кодом (B3a/b/c + B1, OCR on,
гейт OCR ШАГ 0 зелёный). Это финальный ответ на вопрос **«сколько размечать»**.

## ГЛАВНОЕ ЧИСЛО

| | задач | критических |
|---|---:|---:|
| **ОСНОВНАЯ очередь человеку** (дедуп, шум отсеян, ген-догадки в триаж) | **3237** | **73** |
| — критические с кандидатом (actionable, размечать первыми) | 32 | 32 |
| — коды icd/atc/tnm без кандидата (нужен эксперт) | 41 | 41 |
| — термины (eng-OCR/self-repair кандидат → подтвердить) | 3153 | — |
| ген-догадки в ЭКСПЕРТНЫЙ триаж (рус.аббрев+цифра: МО4/ВГВ2/СД2/ФНО8α) | 1186 | — |
| ШУМ отсеян (не человеку) → `_noise.jsonl` | 1083 | — |

**Было 8872 (6634 уник.форм, 1379 крит.). Стало 3237 основных / 73 критических — в 2.7×
меньше человеку, критических в 19× меньше.** (Числа после фиксов Cowork-ревью 2 и 3 — см. ниже.) Причина падения критических: 1306 «критических»
прошлой очереди были ген-догадки формы (рус.аббревиатуры с цифрой), которые детектор
`_looks_critical_term` кастовал широкой сетью; они не блокируют релиз и уведены в триаж.

Разбивка отсева шума (1320): номера страниц/счётчики 522, реальные рус.слова (pymorphy) 461,
одиночные служебные буквы 186, фрагменты рус.стоп-слов (`с и`) 111, единицы (CO2/pCO2/ОФВ1) 40.

## КАК ОТКРЫТЬ В LABEL STUDIO (одна команда, из корня проекта)

```
label-studio start latin_verify --init \
  --input-path _corpus/verify_queue/tasks_min.json \
  --label-config _corpus/verify_queue/labeling_config.xml \
  --input-format json-task
```
Затем http://localhost:8080. Каждая задача несёт ТРИ кропа (слово ≈600 DPI / строка / страница с
подсветкой bbox), исходный текст, предложенную замену и кандидатов. Кнопки разметчика: принять /
выбрать кандидата / ввести свой / «исходник верен» / «неразборчиво» / «нужен медэксперт» + тип
сущности. Экспорт решений → reviewed rules (промпт 14, тот же проект/формат).

## ФАЙЛЫ

- `_corpus/verify_queue/tasks_min.json` — ОСНОВНАЯ очередь (3549), критические первыми.
- `_corpus/verify_queue/tasks_gene_triage.jsonl` — ген-догадки (1186), экспертный триаж.
- `_corpus/verify_queue/_noise.jsonl` — отсеянный шум (1320), не терять, но не человеку.
- `_corpus/verify_queue/crops_min/` — предгенеренные кропы (3 уровня; gitignored).
- `_corpus/verify_queue/labeling_config.xml` — конфиг Label Studio (3 кропа + кнопки).

## РЕЛИЗ-БЛОКИРОВКА (карантин, отдельно от очереди)

Неразрешённых КРИТИЧЕСКИХ латинских спанов (гейт `LATIN_UNRESOLVED`, сильный код-alphabet-
violation в валидном ICD/ATC/TNM): **100 спанов в 67 документах**. Эти документы НЕ едут в
обучение, пока спаны не разрешены (fail-closed, 13b Р4). Это правильная работа, не ошибка.

## СТАТУС КОРПУСА И РЕГРЕССИЯ (ШАГ 5)

- Статусы `outout_latin`: **493 PASS / 146 REVIEW / 62 FAIL** (baseline 495/144/62). −2 PASS /
  +2 REVIEW объяснены +2 unresolved_critical (98→100) через LATIN_UNRESOLVED — fail-closed в
  правильную сторону, FAIL цел.
- Статусы: **494 PASS / 145 REVIEW / 62 FAIL**; unresolved_critical (карантин) **99 в 66 док**.
- Авто-доля восстановления: **75.2%** (auto 8588 / needs_review 2834) — после фиксов предлогов и
  рус.аббревиатур (рус.токены больше не флагаются как порча → чище очередь).
- Скринер precise (vs pre-B3 baseline): **−925** (digit_in_cyrword −575, homoglyph_in_latin
  −170, impossible_start −172, adjacent_mixed −12; shattered_latin +4 — шум-полоса, разобрано).
- Контрольная группа I1 (802/845/901/1000/66/876): все PASS, все 6 байт-идентичны backup.

## ЧТО НА АРБИТРА / СЛЕДУЮЩУЮ СЕССИЮ

- **VLM-арбитр** (`ai6-qwen3-vl-30b`, роутер `mosai-llmrouter.emias.ru` — ДОСТИЖИМ, ключ рабочий):
  запускать пост-хок по precise-флагам под AND-гейтом I28/I29 (арбитр + независимый OCR по токену
  + шаблон сущности; `_source_is_russian` защищает Group B). Замер малой выборки — ниже (ARBITER).
  VLM НИКОГДА не молчаливый авторитет: даёт `system_proposal`, критические — только человек.
- **Ген-триаж** (1186) — медэксперт различает рус.аббревиатуру (ВГВ2=HBV, ФНО=TNF) от реального
  кода/гена; автоматике не под силу (нет медицинского лексикона, I25).
- **Промпт 14** (gold-разметка для МЕТРИК) — отдельная задача, тот же проект Label Studio.

## ФИКС Cowork-ревью 2 (ДО разметки) — третий класс тихой порчи закрыт

Cowork-ревью нашло: B3a/B3c авто-латинизировали ОДИНОЧНЫЕ русские предлоги, когда шаблон/сосед
несёт лат.букву в слоте. Аудит auto-набора: **88 таких** (41 B3a + 47 B3c). Материально:
КР1028_1 «а у 39%»→«MRSA 39%» (патоген, которого нет), КР942_1 «enterocolitica **у** детей»→«…**Y**
детей», класс «**с**»(with)→c (~30). Исправлено (`_RU_PREP`={с,у,а,и,в,к,о,я,б,ж}): предлог →
needs_review, не auto; GAP из одних предлогов («а у») отвергается; ЗАГЛАВНЫЕ генотипы
(Hepatitis В/С, Influenza А) сохранены. **Перегенерация → аудит: 88 → 0** (осталась 1 «ложная»
метка = легит `Hepatitis С у 1 ш з`→virus, где «у» — осколок с ш/з). Подтверждено: КР1028_1 MRSA
убран, КР942_1 Y убран, КР1_4 §2.3 цел (C×3, B×2). I1 байт-идентична. INVARIANTS I43,
`test_prep_protection.py`. **Разметку можно начинать — auto-набор больше не содержит порчи предлогов.**

## ФИКС Cowork-ревью 3 (ДО разметки) — четвёртый класс тихой порчи закрыт

Cowork-ревью нашло: B1 (`ocr_eng_perword`) прогонял eng-OCR по ЛЕГИТ рус. смешанно-регистровой
аббревиатуре и авто-заменял ТРАНСЛИТ-МУСОРОМ: `микроРНК`→`MUKPOPHK` (КР159_2), `аутоТГСК`→
`ayTOTTCK` (КР137_2), `ЭндоУЗИ`/`РостГМУ`/`аллоТГСК`/`КрасГМУ`, `заболеваниях`→`3a00meBaHHAx`
(~20-40). Причина: обе OCR-ноги транслитерируют кириллицу одинаково → B1 «подтверждает» мусор
(I28). Исправлено ИСТОЧНИКОВЫМ языковым гейтом (`_is_ru_mixcase_abbr` + pymorphy на `_cyr_orphan`/
`_protected`/`_c5_protected`) — а НЕ результат-гейтом (он ложно резал бы `ТгапзрП`→Transpl).
Порченая латиница (`КатоРзку`→Karnofsky) не затронута. **Перегенерация → полный аудит auto-набора
против источника: 0 предлогов, 0 рус.аббревиатур, 0 цифро-цитат, 0 eng-OCR рус.слово→мусор**
(осталось легит: 205 одиночных генотипов С→C, 112 a2_template МО→M0 в TNM — entity-валидные).
INVARIANTS I44, `test_mixcase_abbr.py`.

**ИТОГ 4 аудитов против источника (все ревью, не тесты):** цифро-цитаты (15), предлоги B3a/B3c
(88), рус.аббревиатуры (~30) — все закрыты. Правило: любой auto-канал латинизации гейтит ИСТОЧНИК
на язык (рус.слово/предлог/аббревиатура → не auto). Аудит всего auto-набора против источника —
обязателен.

## ARBITER — замер малой выборки (VLM `ai6-qwen3-vl-30b`, AND-гейт I28/I29)

Запуск `vlm_pilot.py 30 40` (30 контроль Group B + 40 выборка очереди), temperature=0,
structured output (закрытый список KEEP_ORIGINAL/CANDIDATE_A/CANDIDATE_B/ABSTAIN):

- **БЕЗОПАСНОСТЬ Group B: 0 / 30 ложных латинизаций русского → GO.** Арбитр на ВСЕХ 30 кропах
  подтверждённо-русских слов (pymorphy-known, из битых док., с соблазном eng-чтения) выбрал
  KEEP_ORIGINAL. Полный AND-гейт (нога языка `_source_is_russian` + независимый OCR + шаблон)
  не латинизировал ни одного. Русский защищён.
- **ЦЕННОСТЬ: 27.5% (11/40) выборки очереди AND-гейт перевёл бы в auto.** Примеры (все
  подтверждены и арбитром, и независимым OCR по токену): `ШСС→UICC`, `трепонема→pallidum`,
  `→Bacteroides`, `→RIPASA`, `→RUSSCO`, `→gonorrhoeae`, `→Grossman`, `→NEC`. Остальные 29 — в
  очередь (арбитр ABSTAIN/KEEP или OCR не подтвердил) — подсказка человеку, замена НЕ применяется.

**Вывод:** арбитр под AND-гейтом мог бы сократить ОСНОВНУЮ очередь ещё на ~27% БЕЗ риска Group B.
НЕ применено в этой сессии осознанно: (1) план — «маленькая выборка» (замер, не масс-прогон);
(2) «VLM НИКОГДА не молчаливый авторитет» — применение к ~3465 терминам = 3465 VLM-вызовов +
перегенерация, решение следующей сессии; (3) all-caps авто (ШСС→UICC) арбитр помечает на доп.
проверку человеком (c5_ambiguous). «Ноль авто арбитром» тоже был бы валиден — но замер показал
реальную ценность при нулевом риске.
