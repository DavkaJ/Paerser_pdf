# Отчёт автономного дневного прогона — 20.07.2026

**Исполнитель:** Claude Code. **Ветка:** `audit-fix-2026-07`. По `_corpus/DAY_RUN_2026-07-20.md`.
Baseline (истинный, OCR-on): **530/130/62** (Jul-14 был 529/130/63; единственная дельта — моя
починка КР715_2).

## 1. Коммиты этого прогона (8; каждый — исполняющий тест I30 + изолированный A/B)

| Коммит | Что | Итог (A/B без дрейфа) |
|---|---|---|
| f4c0785 | Фаза 0.1 furniture по ГЕОМЕТРИИ (I30 #6) | I12 подтверждён; −1 furn/+1 lost; I1 байт; 0 флипов |
| e4a3248 | Фаза 0.2 release из staging + output_sha256 (I30 #7) | реальный `--run`: 529 из staging; дрейф байта → отказ exit 2 |
| 10c3e28 | Фаза 1 ШАГ 1 диагностика content-start | CORE 24 потери глав; watchlists подразделов(56)/FM(19) |
| 2d22f0c | Фаза 1 ШАГ 2 START_IN_TOC (page-tail TOC) | **КР715_2 FAIL→PASS, recall 2→7**; A/B: 1 док, 0 регрессов |
| ac5d03a | (ошибочный baseline — см. §3) | ОТМЕНЁН bda27b2 |
| 587825d | триаж пересчёт | КР715_2 → OK |
| bda27b2 | Baseline перегенерация С OCR — истинный 530/130/62 | исправление §3 |
| 6748022 | Фаза 1 ШАГ 4 numbering model-select (roman) | **КР1021_1 recall 2→7, КР848_1 0→4** (коллапс I10 починен); A/B: 2 док, 0 регрессов |

## 2. Восстановленный контент (главная ценность — вернули потерянные главы)
- **КР715_2** (ШАГ 2): TOC свёрстан номерами страниц без лидеров → тело начиналось пунктом
  оглавления «3.1.2 Физическая активность 58», потеряны главы 1-7. → **7 глав, FAIL→PASS**.
- **КР1021_1** (ШАГ 4): римская нумерация (V=гл.1 … XI=гл.7) отключалась safe-гейтом при
  одиночно-арабском подразделе → 2 секции. → **recall 2→7** (все главы; FAIL по др. причинам).
- **КР848_1** (ШАГ 4): ложный PASS I10 (1 секция, coverage 100%, переучёт 61551>54047). →
  **5 секций, recall 0→4, честный FAIL(MISSING); OVERCOUNT побочно починен** (ratio 1.0005).

## 3. ВАЖНАЯ НАХОДКА и самокоррекция (fail-open I5 сработала на МНЕ)
Первый baseline-прогон (ac5d03a, «522/140/60, 8 PASS→REVIEW дрейфа») был **ОШИБОЧНЫМ**:
`batch.py` запущен в шелле БЕЗ `TESSERACT_CMD` (env не переносится между Bash-вызовами) →
`_resolve_tesseract()`=None → **ГИБРИД-OCR не работал** для born-digital док. Восстановление
порчи не произошло → RESIDUAL_PIN/CORRUPTION всплыли → ЛОЖНЫЕ 8 PASS→REVIEW. Ровно дыра I5
(batch.py без `--require-ocr`). Доказательство: КР1_4 `disagreements`=0 в том прогоне, **486**
с OCR. **Перегенерация С OCR дала истинный baseline 530/130/62 — ноль дрейфа, единственная
дельта КР715_2.** Правило записано в INVARIANTS I34: корпусный прогон — экспортировать
`TESSERACT_CMD` В ТОМ ЖЕ вызове и проверять `ocr._resolve_tesseract() is not None`.

## 4. Скринер порчи (recall-детектор / гейт) — характеристика на outout (latin OFF, OCR on)
`corruption_screen.py` (был баг `DO_OOV` undefined — починил дефолтом `False`). PRECISE-хиты
в ПРОДУКТОВОМ (latin off) корпусе: adjacent_mixed **5054**/454док, digit_in_cyrword **3663**/305
(ATC с кир.буквой, `М01А`), shattered_latin **2874**/432 (`virus`→`у 1 ш з`), impossible_start
**735**/77, homoglyph_in_latin **191**/52. **638/722 док с precise-хитом.** Baseline сохранён
`_corpus/screen_outout_baseline.json`; флаги `_corpus/screen_flags.jsonl` (12517). Это объём,
который берёт Track B (latin recovery).

## 5. Latin (Track B) — измерено на текущем канале (новые каналы не начаты)
`outout_latin` перегенерирован текущим (post-I30-fix) кодом + OCR (`batch_latin.py`). Итог:
- **статусы latin-on: 495/144/62** (PASS НИЖЕ 530 — это fail-closed: гейт LATIN_UNRESOLVED
  карантинит **65 док. с 98 неразрешёнными КРИТИЧЕСКИМИ** латинскими кодами, не пускает в
  обучение порченые ATC/МКБ);
- замен **11780: auto=6427 (применены), needs_review=5353 (в очередь, В ТЕКСТ НЕ применены —
  контракт I30)**; 539/701 док. с правками;
- **очередь верификации ПЕРЕСОБРАНА: 8872 задачи** → `_corpus/verify_queue/tasks.json` (+кропы).

**Скринер-регрессия (гейт DAY_RUN, precise-хиты обязаны падать) — outout_latin vs outout:**
precise **12517 → 11071 (−1446, ~11.5%)**, БЕЗ роста ни в одном ведре (регресса нет). По вёдрам:
digit_in_cyrword **3663→2601 (−1062)** (ATC-коды с кир.буквой — основной эффект A2-шаблона),
adjacent_mixed 5054→4847 (−207), shattered_latin 2874→2753 (−121), impossible_start 735→696
(−39), homoglyph_in_latin 191→174 (−17). **Остаток 11071 precise (603/701 док.)** — доминируют
shattered_latin/adjacent_mixed, которые текущий канал НЕ берёт; это цель НОВЫХ каналов B3
(intra-doc self-repair, пословный/пофразный eng-OCR кроп) — они НЕ реализованы в этой сессии.

**Баги скринера (инструмент Cowork) — починены, чтобы использовать как гейт:** (1) `DO_OOV`
не определён без `--oov` → дефолт `False`; (2) значение `--baseline <path>` парсер считал
позиционным SRC → SRC клоббился на baseline-json (давал ложный «0 файлов, −всё»). Пофикшено
пропуском значения; регрессия теперь честная.

## 6. Что НЕ сделано и почему (честно)
- **ШАГ 3 START_IN_FRONTMATTER**: класс потери глав ПУСТ (диагностика: 0 док с FM-разделом И
  потерей глав). Осталась низко-severity склейка `excluded.toc` (данные, не потеря контента) —
  отложена как наименьшая ROI.
- **ШАГ 5 каноничность (startswith)**: трогает ВСЕ 722, риск широкой регрессии; не потеря
  контента, а precision-робастность. Требует отдельного аккуратного A/B — отложен.
- **B новые латинские каналы (per-word crop / intra-doc self-repair / арбитр)**: инфраструктура
  и скринер-гейт готовы; сами новые каналы — следующий шаг (нужен свежий outout_latin как
  измеряемая база, он генерируется). НЕ начаты в коде.

## 7. Стоп-условия E1/E2/E3: НЕ сработали. Регресс-набор 111 passed / 1 xfailed.

## 8. Что осталось для человека / следующей сессии
- **verify_queue (8872 задачи)** — needs_review-замены (5353) + 98 критических неразрешённых.
  Это то, что человек открывает в Label Studio. НИ ОДНА needs_review НЕ применена в текст.
- **Новые латинские каналы B3** (intra-doc self-repair, пословный/пофразный eng-OCR кроп,
  снятие freq-защиты гомоглифа) — берут остаток 11071 precise (shattered_latin/adjacent_mixed).
  Гейт готов: `corruption_screen.py outout_latin --baseline _corpus/screen_outout_baseline.json`.
- **ШАГ 5 каноничность** (startswith→полное совпадение, все 722, дельта ≤~30) — отдельная сессия.
- **ШАГ 3 склейка excluded.toc** — низкая severity (данные, не потеря контента).

## 9. Вердикт
Основная ценность дня — ВОЗВРАТ ПОТЕРЯННЫХ ГЛАВ — выполнена для всех born-digital кандидатов
(КР715_2 FAIL→PASS; КР848_1 ложный-PASS-коллапс I10 и КР1021_1 разобраны в честную структуру).
Поймана и исправлена СОБСТВЕННАЯ ошибка (baseline без OCR — урок I5), истинный baseline 530/130/62,
ноль регрессов. Latin: текущий канал измерен честно (−1446 precise, digit_in_cyrword −1062),
очередь пересобрана (8872), 65 док. с критической порчей в карантин LATIN_UNRESOLVED — НИЧЕГО
грязного молча в текст. Новые латинские каналы и ШАГ 5 — следующая сессия (инфраструктура и
гейты под них готовы). Затык: НЕТ. Всё спорное помечено и в очереди/карантине.
