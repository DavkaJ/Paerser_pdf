# ROADMAP: до готового корпуса. Статусы на 22.07 14:00

## Цель
JSON-корпус КР, пригодный для генерации синтетики: структурно верный + текст либо чист, либо
помечен (никакого молчаливого мусора), ссылки/карантин исключены.

## Ресурсы (важно)
- **Опус-агенты (Claude Code): ≤4**, только оркестрация. Веерные ultracode-прогоны запрещены
  (169 агентов выжгли лимит).
- **Серверные модели: 8:00–22:00**, внутренний роутер (emias). Весь ОБЪЁМ (классификация,
  предсказания по очереди) — сюда, не на Опус-агентов.

## Текущее состояние
outout_latin: 530 PASS / 130 REVIEW / 62 FAIL. Текст корпуса ещё НЕ очищен (правки в очереди/
провенансе, битые формы типа `ВАР1` на месте). Очередь: 2604 основных + 647 mixcase.

## Шаги

### Шаг 0 — латиница + чистка очереди — ✅ ГОТОВО
Каналы восстановления, 4 класса молчаливой порчи закрыты, очередь ужата 8872→2604, URL-фильтр,
реклассификация, mixcase-арбитр, LLM-пре-аннотации, 72 критических пере-сверены по кропам.

### Шаг 1 — применить проверенные правки к ТЕКСТУ корпуса — ✅ ГОТОВО (2026-07-22, коммиты 31758d3, 5035548)
Пере-собран из ПОЛНОГО набора LS latin_verify (170 аннотаций) -> 154 merged (старое побеждает,
сохранена пере-сверка критических) -> **гейт безопасности** отложил 22 неоднозначные формы
(инициалы Е.А/Р.А, короткие фрагменты ап/апб=anti, фразы-предлоги `difficile с`=with) в
`_deferred_ambiguous.json` -> **132 применено** через движковый оверлей + перепарс 97 док. с OCR,
854 замены. 3 невалидных ATC -> `_requeue_critical.json`. Аудит airtight (OFF/ON в одном процессе,
мультимнож. токен-диф): **97/97 чисто**, I1 OFF==ON==disk байт-в-байт; скринер digit_in_cyrword -63.
Отчёт: `_overlay_delta_report.md`. Инструменты: `rebuild_verified_from_ls.py`,
`safety_filter_verified.py`, `apply_and_audit_final.py`. Память: [[overlay-apply-step1]].

### Шаг 2 — дорешать очередь ревью (2604 v4 + 647 mixcase) — 🔄 В РАБОТЕ
ОБЪЁМНЫЙ LLM-проход на СЕРВЕРНЫХ моделях (роутер emias, 8–22ч), НЕ Опус-агенты:
`llm_prefill_queue.py`. v4 (кропы) -> VLM-арбитр `ai6-qwen3-vl-30b` (закрытый список + независимый
OCR + языковой гейт = auto/queue/keep); mixcase (контекст, без кропа) -> текстовый арбитр
`ai1-medgemma-27b` (gpt-oss/minimax отдают content=None). Предсказания -> `_v4_predictions.jsonl`
+ `_mixcase_predictions.jsonl`. В ТЕКСТ не пишем, в LS НЕ импортируем, пока не собрана финальная
очередь. Сюда же уйдут 22 отложенные неоднозначные формы (per-occurrence). Человек подтверждает
критические/спорные по кропу.

### Шаг 3 — доказать чистоту («ничего молча битого») — ⏳ TODO
Разобрать пере-очередь (3 ATC + класс «оба пропустили»). Прогнать скринер по перегенерированному
корпусу: precise-порча → ~0 или всё помечено. Гейт перед экспортом.

### Шаг 4 — структурный релиз-контракт — ⏳ TODO (частично)
62 FAIL: дочинить content-startّом ИЛИ карантин (не тащить сломанную структуру в синтетику).
Таблицы (аудит: recall 35–70%) — принять best-effort или пометить. Это НЕ латиница — отдельная дыра.

### Шаг 5 — экспорт под синтетику — ⏳ TODO
Применить контракт: выкинуть `excluded.references`, карантин (FAIL/REVIEW/неразрешённые критические),
needs_review-спаны; оставить confidence-флаги. Выход: чистый набор + манифест (`synthetic_ready`).
→ после этого корпус можно отдавать в генерацию.

### Шаг 6 — gold set + аттестация — ⏳ НЕ НАЧАТ (для ДОКАЗАННОГО качества, отдельно)
30–50 стратифицированных док. со span-разметкой + харнесс метрик → цифра готовности. Для синтетики
не строго обязателен, но без него нельзя сказать «корпус на X% верен» с числом. Инфраструктура
(LS, кропы, предсказания) уже стоит.

## Порядок
1 (сейчас, без моделей) → 2 (серверные модели, объём) → 3 (гейт чистоты) → 4 (структура) →
5 (экспорт). 6 — параллельно/после, когда будет ручной ресурс на разметку.
