# Пере-сверка 72 критических (LLM vs КРОП) — 21.07 (заход с лимитом ≤4 агента)

Метод: для каждой из 72 критических задач сверял **эффективную ручную форму** (`resolution.corrected_text`
если decision=ошибка, иначе принятый кандидат) против **LLM-предсказания** (`calib_pred.json`) под
канонической латинской нормализацией. Реальные расхождения (7 символьных + невалидные коды) сверял
**по кропу пикселями** (у меня зрение). `_resolved_human` эталоном НЕ считал. Результат по каждой задаче —
в `verify_queue/_resolved_final.jsonl` (провенанс на каждую).

## Главный вывод — премиса ревью 4 на ТЕКУЩИХ данных НЕ подтвердилась
Ревью 4 утверждало «LLM точнее человека на кодах, брать LLM». По факту ночная ручная разметка
(`corrected_text`, экспорт 20:06) **чистая и корректная**: `BAP1` (не BAPl), `L04AB02` (не LO4AB02),
`SMARCB1` (не SMARCBI). Ошибки O/0·I/1, которые цитировало ревью, относятся к МАШИННОМУ кандидату
(`resolved_text`), а не к тому, что ввёл человек. Ревью сравнивало не с тем полем / со старым срезом.

**В КАЖДОМ реальном расхождении прав человек, а LLM (calib_pred) ошибался или обрезал:**
- `lt_00048` `рМ1а`: кроп = **pN1a(sn)** «Микрометастазы… биопсии сторожевых» → это N (узловая), а не M.
  Человек `pN1a` ✅; LLM `pM1a` ❌ (M↔N — крупная клиническая разница).
- `lt_00047` `Уо1.22`: кроп = «Radiology. 2002. **Vol. 224**» → человек ✅; LLM `Vol.22` обрезал.
- `lt_00055` `Уо1.1`: кроп = «J. Clin. Oncol. 1997. **Vol. 15**» → человек ✅; LLM `Vol.1` обрезал.
- `lt_00026` `гЦТ11Ш1с!еПпе`: кроп = «…/PDF/**rUTIguideline**» (URL) → человек ✅; LLM `guideline` частично.

→ Правило «брать LLM поверх человека» на этих данных **внесло бы ошибки**. Кроп — единственный арбитр.

## Дефекты, которые не поймал НИ человек, НИ LLM (нашёл кропом + доменным знанием)
**3 невалидных ATC — оба оставили букву O там, где в ATC цифра 0** (result-validity нарушен):
- `lt_00060` `СО2АВО2` → **C02AB02** (метилдопа рацемич.) — контекст «метилдопа», в строке рядом `C08DB01` с нулём.
- `lt_00061` `СО2АВО1` → **C02AB01** (метилдопа левовращ.)
- `lt_00062` `СО8САО2` → **C08CA02** (фелодипин)

Эти три — критические коды, я НЕ применяю их молча: `disposition=requeue_correction` → назад в очередь
с моим предложением на подтверждение человеком.

## Реклассификации (ложно-критические — не коды)
- Цитаты `Vol.NN` (`lt_00039/47/55`) tagged icd → not_entity (библиография, шум).
- URL-фрагмент (`lt_00026`) → not_entity (URL-шум).
- Номер приказа `№585нн` (`lt_00069`) tagged atc → not_entity (шум).
- Физио-термины `pO2/H2O/VO2` (`lt_00035/36/40/46/67/70`) tagged icd → term, non-critical.
- Рус. `500ЛЕ` (доза), `500кл` (клетки), `МО4` (молек.ответ) tagged atc/icd → keep-cyr, non-critical.
- `5HT3` (`lt_00064`) tagged atc → на деле рецептор/класс (форма верна, тип неверен).

## Итог по диспозициям (72 крит.)
| disposition | n | применять к тексту? |
|---|---|---|
| apply_two_source_agree | 40 | да (human_verified + llm_agree, форма валидна) |
| apply_homoglyph_normalize (TNM М1а→M1a …) | 12 | да, латинизация (это ложн.срабат. mixcase-гейта, п.1) |
| apply_human_crop (pN1a, A05, J01XX) | 3 | да (кроп подтвердил человека) |
| requeue_correction (C02AB02/01, C08CA02) | 3 | **нет** — назад в очередь человеку |
| reclassify_physio_term | 6 | реклассификация (term, non-crit) |
| reclassify_citation/url/order_noise | 5 | в шум |
| reclassify_keep_cyr | 3 | оставить рус., non-crit |

**Итого к применению в текст: 55** (40+12+3), **на подтверждение человеку: 3**, **реклассификация: 14**.
Провенанс каждой записи — в `_resolved_final.jsonl`. Кропы читал: lt_00026,34,47,48,52,55,60,61,62,63,65,68,69.
