# 01b. Патч уже выполненного промпта 01 (коммит `58968f0`)

Промпт 01 исполнен, и в основе — правильно: 722/722, 616 PASS / 58 FAIL / 48 REVIEW,
57 681 страница, зафиксирован рассинхрон выгрузки (596 файлов вместо 616, КР875_1 со
статусом REVIEW лежит в PASS-папке). Манифест снят, дрейф найден.

**Два дефекта, подтверждённые проверкой. Оба чинить до промпта 02.**

**Что НЕ является дефектом (не «чинить»):** `--verify` возвращает **exit 1**, а не 0 —
проверено прямым запуском. `tree_sha256` изменился -> `diverged = True` -> `return 1`.
То, что `git_sha` только печатается и не поднимает флаг, — **правильно**: коммит самого
манифеста меняет `git_sha`, не меняя кода. Не трогать.

---

```
Задача: два точечных исправления в результате промпта 01. Больше ничего не менять.

=== ДЕФЕКТ 1. FINAL_REPORT.OBSOLETE.txt испорчен ===
Файл раздут с ~3,5 КБ до 45 321 байт: предупреждающая шапка повторена **72 раза**
(похоже, вставка применилась построчно, а не один раз в начало файла).
Фикс:
  - восстановить исходное содержимое отчёта (из git: `git show 5a5869a:_corpus/FINAL_REPORT.txt`
    — если он там есть; иначе взять любую копию до правки и убрать все дубли шапки);
  - шапка вставляется РОВНО ОДИН РАЗ, первым блоком, затем пустая строка, затем исходный
    текст отчёта БЕЗ изменений;
  - проверка: `grep -c "УСТАРЕЛО" _corpus/FINAL_REPORT.OBSOLETE.txt` == 1;
    размер файла — порядка 3,5-4 КБ, не 45.

=== ДЕФЕКТ 2. cmd_verify сверяет не всё, что пишет в манифест ===
Сейчас `cmd_verify` диффает только: inputs, outputs, code(tree_sha256), pins, registry.
В манифест при этом ПИШУТСЯ и НЕ ПРОВЕРЯЮТСЯ: `env`, `config`, `report_sha256`, `counts`.
Это дыра: смена версии Tesseract, traineddata, Pillow, PyMuPDF или DPI/воркеров пройдёт
молча — а именно они ломают воспроизводимость OCR, ради которой манифест и делался.
Фикс — добавить в `cmd_verify` четыре группы сравнения, каждая поднимает `diverged`:
  - `env`: сравнивать поэлементно (python, pymupdf, pdfplumber, pillow, pytesseract,
    fonttools, tesseract.version, tesseract.traineddata[*].sha256). Печатать КАЖДОЕ
    расхождение отдельной строкой: «[env] tesseract.version: 5.4.0 -> 5.5.1».
    ВАЖНО: путь к traineddata (`path`) сравнивать НЕ НАДО — он машинозависим. Сравнивать
    только sha256 содержимого.
  - `config`: ocr_dpi, max_workers, cov_fail, cov_warn.
  - `report_sha256`.
  - `counts`: inputs, outputs, pages_total, pass, fail, review.
Каждая группа — отдельная строка вывода, чтобы расхождение можно было атрибутировать.

=== ГРАНИЦЫ ===
- Трогать: crparser/engine/manifest.py (только cmd_verify), _corpus/FINAL_REPORT.OBSOLETE.txt.
- НЕ менять: build_manifest, snapshot, drift, формат манифеста, сам baseline_manifest.json.
- НЕ пересобирать baseline (он корректен). Только сверка стала строже.
- НЕ трогать логику git_sha (она верна).

=== ТЕСТЫ-ПИНЫ ===
- `grep -c "УСТАРЕЛО" _corpus/FINAL_REPORT.OBSOLETE.txt` == 1; размер < 5 КБ; исходный
  текст отчёта (623/700 PASS, 77 FAIL) в файле присутствует полностью и читаем.
- `--verify` на неизменённом окружении: расхождений по env/config/counts/report нет.
- Искусственно изменить `config.ocr_dpi` в baseline_manifest.json с 400 на 300 ->
  `--verify` печатает «[config] ocr_dpi: 300 -> 400» и возвращает 1. Вернуть.
- Искусственно изменить `counts.pass` -> `--verify` ловит и возвращает 1. Вернуть.
- `--verify` по-прежнему возвращает 1 из-за tree_sha256 (код изменился — это ожидаемо
  и правильно). Прямой запуск, БЕЗ пайпа: `python -m crparser.engine.manifest --verify;
  echo $?`. Пайп в `tail`/`head` подменяет код возврата — не тестировать так.

=== РЕГЛАМЕНТ ===
1. Исправить оба дефекта одним коммитом.
2. Пересоздать baseline НЕ НАДО. После коммита `--verify` будет показывать изменение
   tree_sha256 — это норма, код действительно изменился. Baseline пересниматься должен
   только по явному решению, с записью причины.
3. Коммит: «Манифест: полная сверка env/config/counts/report; восстановить устаревший отчёт».
```
