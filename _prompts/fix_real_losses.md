# Фикс: реальная потеря контента (КР603_3, КР655_2, КР716_1); КР999_1 — known-issue

Подтверждено на живых данных. Корни уточнены: КР603_3 — канонический заголовок главы 1 БЕЗ номера в теле не распознаётся (не дубль!); КР655_2 — тот же симптом, но гашение как фантом-дубль (проверить эмпирически, иначе known-issue); КР716_1 — буллит «профилактика пролежней;» промотируется в главу 5 по префиксу; КР999_1 — потеря в зоне вычитания таблиц + два «3.2», в патч НЕ входит. Это core-логика заголовков — гейт нулевой регрессии обязателен.

---

```
Контекст: парсер КР Минздрава (PDF → JSON), профиль crparser/profiles/clinical.py,
движок crparser/engine/segmenter.py. 596 файлов PASS. Нужно починить РЕАЛЬНУЮ
потерю контента в 2 файлах (КР603_3, КР655_2 — общий симптом) и в 1 файле
(КР716_1), с НУЛЕВОЙ регрессией. КР999_1 — не чинить, это known-issue (ниже).

Подтверждено на живых данных (outout/<file>.json, _corpus/report.json, pdftotext
-layout data/raw/<file>.pdf). Проверяй сам, не доверяй пересказу.

===================================================================
БАГ 1 (КР603_3, КР655_2). Пропадает вся глава 1 «Краткая информация» вместе с
подразделами 1.1–1.5 (Определение/Этиология/Эпидемиология/Кодирование/Класси-
фикация). В outout дерево обоих файлов начинается сразу с главы 2 «Диагностика».

  Факты источника:
   * КР603_3: в ТЕЛЕ глава 1 — ГОЛЫЙ канонический заголовок БЕЗ номера:
     "Краткая информация по заболеванию или состоянию (группе заболеваний…)"
     (в оглавлении она под римской "V."). Подразделы точечные: "1.1 Определение…",
     "1.2 Этиология…" … "1.5 Классификация…".
   * КР655_2: в ТЕЛЕ глава 1 есть С номером ("1. Краткая информация…"), подразделы
     "1.1 Определение…" … (текст испорчен леттерспейсингом: "О пределение",
     "Ж алобы"). Оглавление безлидерное (run-on).

  Корень (проверь трассировкой):
   * КР603_3: classify_heading НЕ имеет ветки «канонический заголовок главы без
     арабского/римского номера». Глава 1 не распознаётся → в segmenter max_top
     остаётся 0 → toc_reject режет 1.1–1.5 по условию `_top_int(number) != max_top`.
     Это ОСНОВНОЙ, высоконадёжный корень.
   * КР655_2: глава 1 в теле С номером "1", но, вероятно, гасится как «фантомный
     дубль» в Segmenter._run_state_machine.flush_open (ветка
     `norm(title) == norm(seen_numbers[num])` — эхо оглавления/повтор номера "1"),
     ЛИБО обрезается content-start. ТОЧКУ сброса подтверди эмпирически (лог/трасса),
     прежде чем править. Если единственный безопасный рычаг — content-start,
     НЕ трогай его: вынеси КР655_2 в known-issue и ограничься КР603_3.

  Правильное поведение: канонический раздел ВЕРХНЕГО уровня (Краткая информация /
  Диагностика / Лечение / Медицинская реабилитация / Профилактика / Организация /
  Дополнительная информация) должен привязываться по КАНОНИЧЕСКОМУ ЗАГОЛОВКУ, а не
  только по номеру, и НЕ отбрасываться, даже если:
   * заголовок в теле идёт без номера (взять номер из канонического имени, как это
     уже делает _canonical_chapter_number для римских глав) — чинит КР603_3;
   * его номер верхнего уровня повторяет более раннее (оглавленческое) вхождение —
     сохранить именно ПОДТВЕРЖДЁННЫЙ телом канонический раздел — чинит КР655_2.

  Реализация (узко):
   * В clinical.py добавь ветку распознавания level-1 канонической главы без номера:
     ТОЛЬКО visual + точное совпадение по _canonical_chapter_number (1..7) +
     не _looks_like_body_fragment. Номер синтезируй из канона. Никакого хардкода
     названий сверх уже существующего _MAIN_SECTION_NUMBERS.
   * Проверь, что после этого segmenter выставляет max_top=1 и 1.1–1.5 проходят
     toc_reject (они confirmed оглавлением в 603).

===================================================================
БАГ 2 (КР716_1). В узел главы 5 попал буллит-пункт списка вместо канонического
заголовка. Настоящая глава 5 теряется.

  Факты источника:
   * "5. профилактика пролежней;" (строчная буква, конец на ';', короткая строка,
     это пункт списка в теле) — попадает в дерево как level-1 глава 5.
   * "5. Профилактика и диспансерное наблюдение, медицинские показания…" (реальная
     глава 5) далее отвергается order_ok (номер 5 уже занят буллитом).
   * report: TITLE_MISMATCH 5: оглавление 'Профилактика…' != вывод 'профилактика
     пролежней;'.

  Корень: _valid_main_title матчит по ПРЕФИКСУ ("профилактика" → {5}), поэтому
  "профилактика пролежней" считается КАНОНИЧЕСКИМ, и проверка «заглавная буква»
  в _parse_numbered для level-1 обходится (она под `if not canonical`).

  Правильное поведение: строку-пункт списка НЕ промотировать в канонический
  заголовок главного уровня. Настоящий заголовок главы шаблона всегда начинается
  с ЗАГЛАВНОЙ буквы и не оканчивается на ';'.

  Реализация (узко): для КАНДИДАТА level-1, претендующего на КАНОНИЧНОСТЬ, требуй
  дополнительно: первый символ заголовка — заглавная (или «), И заголовок не
  оканчивается на ';'. Правило применяй ТОЛЬКО к канонической ветке level-1
  (главы 1–7), чтобы не задеть подуровни и неканонические разделы. «профилактика
  пролежней;» → отклонится → реальная «5. Профилактика…» примется.
  3.1.2.8/3.1.2.9 (глубокие подпункты) — вторичная потеря; если её причина не
  вскрывается тем же изменением, НЕ форсируй, отметь как остаточную в отчёте.

===================================================================
KNOWN-ISSUE (НЕ ЧИНИТЬ в этом патче): КР999_1.
  Теряются 2.2/2.3/2.4 (гл.2), 3.1 и 3.4 (гл.3) — все подтверждены оглавлением;
  плюс два разных раздела с номером «3.2» ("3.2 Инъекционная терапия" vs
  "3.2 Хирургическое лечение"; в оглавлении только «Хирургическое»). Утраченные
  подразделы сильно отбиты вправо и соседствуют с таблицами (28 таблиц в файле) —
  вероятная причина в вычитании таблиц, а это запрещённая зона. Разводить два
  «3.2» в общем патче рискованно. Оставить как есть, зафиксировать в отчёте.

===================================================================
ЯВНЫЕ ГРАНИЦЫ (НЕ ТРОГАТЬ):
  * content-start / _find_content_start и логику отсечения ToC/front-matter;
  * детекторы таблиц и вычитание (_covered_by_table, subtraction_map), детекторы
    порчи/леттерспейсинга/OCR;
  * validate.py и любой код отчётности;
  * toc.py bounds — не менять границы оглавления;
  * НИКАКОГО хардкода конкретных файлов/названий болезней/номеров страниц;
  * правила держать МАКСИМАЛЬНО узкими: только канонические главы 1–7 и только
    явный буллит-кейс (строчная/';'). Не расширять радиус на подуровни и
    неканонические разделы.

===================================================================
ТЕСТЫ-ПИНЫ (должны выполниться после фикса):
  * КР603_3: в дереве появляется глава 1 «Краткая информация…» (level 1) с детьми
    1.1–1.5; дерево больше НЕ начинается с главы 2. Нет MISSING по 1.1–1.5.
  * КР655_2: аналогично глава 1 + 1.1–1.5 возвращаются. (Если эмпирически причина
    окажется только в content-start — оставить как known-issue, честно указать.)
  * КР716_1: узел главы 5 = «Профилактика и диспансерное наблюдение…», НЕ
    «профилактика пролежней;». MISSING 5 исчезает. (3.1.2.8/3.1.2.9 — по факту.)
  * КР999_1: не регрессировал (в патч не входит).

ЖЁСТКАЯ РЕГРЕССИЯ — эти PASS-файлы (проверены в report.json) НЕ должны сломаться,
особенно логика канонических глав и дублей/коллизий номеров:
  * КР717_2 — главы 1–7 восстановлены по каноническому имени (EXTRA), есть гл.5
    «Профилактика и диспансерное наблюдение» + 5.1/5.2. Пин на баг 1 и баг 2.
  * КР718_2 — главы восстановлены по канону, гл.5 + 5.1/5.2/5.3. Пин на баг 2.
  * КР9_3 — перенумерация источника + TITLE_MISMATCH + канонические 1.x как EXTRA.
    Пин на устойчивость к коллизиям/сдвигу номеров.
  * КР654_2 — леттерспейсинг + множество EXTRA-подразделов (3.1/3.2/3.2.1…).
  * КР602_2 — чистый базлайн.

РЕГЛАМЕНТ:
  1. Прогнать ПОЛНЫЙ регресс по всему корпусу.
  2. Пересобрать outout/ и _corpus/report.json.
  3. Интеграл-чек: число PASS не уменьшилось (было 596); список FAIL сократился
    ровно на починенные файлы, новых FAIL/REVIEW нет; покрытие не просело.
  4. Коммит ТОЛЬКО при нуле регрессий. Если хоть один прежде-PASS файл сломался —
    откатить правило и сузить.
  5. Сообщение коммита — по-русски, по существу, БЕЗ упоминания Claude/AI и
    БЕЗ Co-Authored-By.
```
