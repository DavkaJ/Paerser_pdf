# Фикс: глубокая вложенность номера (>5 уровней)

Проверено на живых данных: КР1041_1 — иерархия в outout/КР1041_1.json обрывается на уровне 4 (3.1.1.1.x); заголовки 3.1.1.1.2.1.1 «Натрий» и глубже реально есть в теле PDF (pdftotext, стр.1931, 1962, 1994, 2205, 2271, 2445; макс. 7 уровней), в report.json помечены MISSING. Корень: crparser/profiles/clinical.py — квантификатор {0,4} в _RE_NUMBERED/_RE_NUMBER_ONLY (макс. 5 компонентов) и жёсткий потолок `> 5` в _valid_number. Сборщик иерархии segmenter.py::_link работает для любой глубины (родитель по префиксу), его трогать не нужно.

ВАЖНО: КР682_2 («#»-заголовки), КР716_1 (артефакт номера страницы), КР139_2 (приложение/таблицы) — ДРУГИЕ баги, не этот. Пин только КР1041_1.

---

```
Узкий баг: подразделы с ГЛУБОКОЙ вложенностью номера (>5 уровней, напр. «3.1.1.1.2.1.1»)
теряются при парсинге КР. Снять лимит глубины номера. НЕ расширять область — это ровно один баг.

## Где баг (crparser/profiles/clinical.py)
1) Стр. 95: _RE_NUMBERED = re.compile(r"^(\d+(?:\s*\.\s*\d+){0,4})\s*\.?\s*([A-Za-zА-Яа-яЁё«].*)$")
   {0,4} = максимум 5 компонентов. Номера вида 3.1.1.1.2.1.1 (7 уровней) не матчатся.
2) Стр. 101: _RE_NUMBER_ONLY = ^(\d+(?:\s*\.\s*\d+){0,4})\s*\.?$ — тот же {0,4} для висящего номера.
3) Стр. 356-367, _valid_number: `if number.count(".") + 1 > 5: return False` — жёсткий потолок 5.

## Подтверждение (эталон КР1041_1)
pdftotext -layout data/raw/КР1041_1.pdf — заголовки в ТЕЛЕ (номер чистый):
  1931: 3.1.1.1.2.1.1 Натрий
  1962: 3.1.1.1.2.1.2 Калий
  1994: 3.1.1.1.2.1.3 Магний
  2205: 3.1.1.2.3.1 Аминокислоты (группа АТХ - B05XB) (для парентерального введения)
  2271: 3.1.1.2.5.1 Осложнения, ассоциированные с центральным венозным катетером.
  2445: 3.1.1.2.5.2 Осложнения длительного парентерального питания (более 2 недель).
Сейчас в outout/КР1041_1.json иерархия обрывается на уровне 4; в report.json КР1041_1 —
FAIL, 13 записей MISSING (3.1.1.1.2.1.1 … 3.1.1.2.5.2).

## Правильное поведение
Распознавать нумерованный заголовок произвольной глубины N.M.K.L… до санитарного потолка
(напр. 9 уровней). Иерархия по префиксу номера; level = число точек + 1. Сборщик
segmenter.py::_link (стр. 622-646) уже работает для любой глубины — НЕ трогать.

## Что менять (минимально)
- clinical.py:95  _RE_NUMBERED: {0,4} → {0,8}.
- clinical.py:101 _RE_NUMBER_ONLY: {0,4} → {0,8}.
- clinical.py:356-367 _valid_number: `> 5` → `> 9` (или константа рядом с _MAX_TOP_LEVEL).
  Сохранить ВСЕ прочие проверки: _RE_DATE_LIKE (даты), parts[0] в 1.._MAX_TOP_LEVEL, пустой parts.

## ГРАНИЦЫ — НЕ ТРОГАТЬ
- Char-класс начала заголовка [A-Za-zА-Яа-яЁё«] в _RE_NUMBERED (это отдельный баг «#»-заголовков
  КР682_2, вне области).
- Римскую ветку (_RE_ROMAN_HEAD/_RE_ROMAN_ONLY, _parse_roman_*), _roman_ok.
- Детекторы таблиц (tables.py, _RE_SECTION_HEAD — там (?:\.\d{1,3})+ уже без лимита глубины).
- Детекторы порчи/OCR (textnorm.py), _find_content_start / логику content-start, _INLINE_SPLIT.
- validate.py и логику отчёта.
- Прочие условия распознавания заголовка (пробел/точка+текст с буквы; visual/жирность/заглавная
  для подзаголовков; _looks_like_body_fragment; _FORBIDDEN_STARTS; отсечение ToC-лидеров/подписей).
- НЕ решать КР682_2, КР716_1, КР139_2 — другие баги; они не должны стать PASS от этого фикса
  и не должны сломаться.

## Тесты-пины
ДОЛЖНО ПОЯВИТЬСЯ (ранее MISSING):
- КР1041_1: узлы 3.1.1.1.2.1.1 «Натрий», …2.1.2 «Калий», …2.1.3 «Магний», 3.1.1.2.3.1
  «Аминокислоты…», 3.1.1.2.5.1, 3.1.1.2.5.2 — вложены под родителей по префиксу.
  В report.json записи MISSING 3.1.1.1.2.1.* / 3.1.1.2.* исчезают.
НЕ ДОЛЖНО ИЗМЕНИТЬСЯ (регрессия-гард, PASS, есть 4-уровневые подразделы — тот же код-путь):
- КР1006_1 (PASS), КР100_2 (PASS, 100.0), КР1028_1 (PASS, 100.0) — статус/структура без изменений.
- Ни один PASS-файл не должен стать FAIL/REVIEW.

## Регламент
1. Полный регресс (парсер требует PyMuPDF).
2. Пересобрать outout/ и _corpus/report.json.
3. Интеграл-чек: все JSON валидны, нет обрыва.
4. Diff report.json: допустимо только исчезновение MISSING у КР1041_1 (и др. файлов с реально
   глубокими номерами). Ни один PASS → FAIL/REVIEW. Любая новая регрессия — НЕ коммитить.
5. Коммит только при нуле регрессий. Сообщение — по существу, БЕЗ упоминания Claude/AI/ассистента
   и БЕЗ трейлера Co-Authored-By.
```
