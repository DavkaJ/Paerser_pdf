# Фикс: честный валидатор + зрячий детектор порчи (PASS должен значить «чисто»)

Три подтверждённые дыры. Найдена надёжная сигнатура порчи: символ «§» (U+00A7), впаянный ВНУТРЬ буквенно-цифрового токена (Ри§Ь←Child-Pugh, НВзА§←HBsAg, 1§М←IgM). Замер по корпусу: у 611/701 файлов таких токенов ноль; порог даёт ровно 21 файл PASS→REVIEW + КР875_1 (пустой). Здоровые файлы с легит-латиницей (0 §-токенов) не задеты. Ожидаемый сдвиг: PASS 596 → ~574. Правка только в отчётности/детекции — парсер не трогается, можно прогнать по готовым JSON без переパарса.

---

```
Сделать валидатор честным на пустых файлах и зрячим к кирилло-латинской глиф-порче.
Три подтверждённые дыры, чинить ТОЛЬКО в validate.py / textnorm.py / stats.py
(отчётность и детекция). Парсер, сегментер, детектор таблиц, content-start — НЕ трогать.

=== ДЫРА 1. Пустой/бестекстовый файл получает PASS ===
Эталон: КР875_1 — sections_found=0, included_chars=0, total_chars=11152,
coverage_percent=100.0, cls=LOW_TEXT_REVIEW, но status=PASS. Причина: в validate_doc
порог SKIPPED_SCAN = (total_chars<200) ИЛИ (cov<=0 И sec_found==0). Здесь cov=100
и текст «есть» → не скип, ни одного FAIL/REVIEW → PASS. То есть «в разделы не попало
ничего» нигде не проверяется.
Правильное поведение: если sections_found==0 ИЛИ included_chars==0, файл НЕ должен быть
PASS — поднять REVIEW (kind напр. EMPTY_OUTPUT / NO_SECTIONS) с внятным сообщением.
Если текстовый слой действительно пуст (total_chars<200) — остаётся SKIPPED_SCAN.
Ключ: 0 разделов или included_chars==0 → любой статус, кроме PASS.

=== ДЫРА 2. Фиктивный coverage=100% при included_chars==0 ===
stats.py: coverage = accounted/total, accounted = included_chars + excluded_chars +
table_chars (кап по total). У КР875_1 included_chars=0, но excluded/table добирают до
100%. Правильное поведение: не показывать высокий coverage при included_chars==0
(минимально: included_chars==0 → coverage_percent=0.0). Формулу для нормальных файлов
НЕ менять — правка только для вырожденного случая included_chars==0.

=== ДЫРА 3. Детектор порчи слеп к кирилло-латинской глиф-мешанине ===
У ~70 файлов тело/библиография — битый шрифт-слой оригинала: BCLC→ВСЬС, TNM→ТNМ,
Child-Pugh→Ри§Ь, HBsAg→НВзА§, IgM/IgG→1§М/1§С, Allergy→А11ег§у. Текущие счётчики
(glyph_tokens/pseudo_ascii_tokens) этого НЕ ловят (0-1 у всех эталонов) → PASS.
Эталоны порчи (обязаны стать REVIEW): КР263_2 (§ только в references, тело чистое),
КР396_4, КР1_4, КР957_1, КР628_2, КР332_2.

НАЙДЕННАЯ НАДЁЖНАЯ СИГНАТУРА (проверена по 701 файлу): символ «§» (U+00A7), впаянный
ВНУТРЬ буквенно-цифрового токена (рядом с § стоит буква/цифра) — почти всегда битый
глиф. У 611/701 таких токенов НОЛЬ. Считать ПЛОТНОСТНО, по доле таких токенов среди
значимых токенов ВСЕГО извлечённого текста (ОБЯЗАТЕЛЬНО включая references/excluded —
у КР263_2 порча только в библиографии; если считать лишь по телу разделов, не поймается).

Замер (§-токенов / на 1000 значимых токенов):
  КР628_2 344/20.1  КР396_4 311/10.7  КР1_4 185/9.7  КР263_2 46/6.0
  КР332_2 49/4.7    КР957_1 61/4.2
  ЗДОРОВЫЕ КР802_1/845_1/901_1/КР1000_1 → 0/0.0 (при 66-120 ЛЕГИТИМНЫХ лат. токенов).
Единичные § легитимны (турецкие фамилии Demirba§/Be§irli в references, сноски
Цисплатин**§) — отсекаются абс. минимумом + порогом плотности.

Порог (консервативный): REVIEW, если §-токенов >= 5 И доля >= 2.0 на 1000 (0.002).
При этом ровно 21 файл PASS→REVIEW, здоровые не задеты (2x+ запас над самым шумным
здоровым 0.45/1000, и уверенно ловит эталоны — мин 4.2/1000).
ВАЖНО: mixed-токены (кириллица+латиница внутри слова, ТNМ) как САМОСТОЯТЕЛЬНЫЙ триггер
НЕ использовать — у здорового КР1000_1 их 66. Только «§-в-токене» плотностно.

РЕАЛИЗАЦИЯ (в границах трёх файлов):
- textnorm.py: чистые хелперы — предикат «токен с впаянным §» (U+00A7 И хотя бы один
  alnum в токене) и density-gate (по образцу pseudo_ascii_glyph_tokens: счётчик только
  при доле>=порога И абс>=минимума, иначе 0). Знаменатель — значимые токены
  (переиспользовать _is_significant_token). Доменно-нейтрально, без хардкода имён.
- validate.py: собрать текст из doc (обход sections + excluded — там references!),
  прогнать хелпер, при срабатывании rep.review("CORRUPTION", …) с разбивкой. Встроить в
  существующую _corruption_review / rep.corruption. Структурную сверку с TOC не трогать.
- Пороги — именованные константы с комментом о замере по корпусу. Без хардкода КРxxx.

=== ЯВНЫЕ ГРАНИЦЫ ===
- Трогать ТОЛЬКО validate.py, crparser/engine/textnorm.py, crparser/engine/stats.py.
- НЕ трогать парсер разделов/сегментер/детектор таблиц/content-start/чтение PDF.
- НЕ загонять в REVIEW здоровые файлы с легит-латиницей: одиночная латиница, коды МКБ,
  TNM/IgG/E.coli, фамилии в библиографии — НОРМА. Триггер только по ПЛОТНОСТИ §-в-токене.
- Без хардкода имён файлов и списков исключений.

=== ТЕСТЫ-ПИНЫ ===
НЕ PASS: КР875_1 → REVIEW/SKIP (пустой вывод / included_chars==0).
REVIEW по §-порче: КР263_2, КР396_4, КР1_4, КР957_1, КР628_2, КР332_2.
РЕГРЕССИЯ — остаются PASS: КР802_1, КР845_1, КР901_1, КР1000_1 (66-120 лат. токенов,
0 §-токенов). Численно: PASS 596 → ~574. Если PASS падает больше чем на ~25-30 — порог
занижен; если КР263_2 остаётся PASS — плотность считается не по всему тексту (забыты
references).

=== РЕГЛАМЕНТ ===
1. Прогнать валидатор по всему корпусу (можно по готовым JSON из outout + пересбор
   report.json; парсер перезапускать НЕ обязательно).
2. Интеграл-чек: дельта PASS→REVIEW = ~21 §-файл + КР875_1; ни один из 4 регресс-эталонов
   не уехал.
3. Коммит только при НУЛЕ ложных REVIEW на здоровых. Сообщение — по существу, без
   упоминания Claude/AI и без Co-Authored-By.
```
