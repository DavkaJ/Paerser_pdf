# OCR-восстановление: максимальная полнота (полностраничный OCR кандидата + словарь сокращений + база пинов)

Тестировать ТОЛЬКО на 2 файлах: КР1_4 и КР628_2 (+ КР1000_1 как регресс). НЕ гонять весь корпус.

Точки интеграции (проверены в коде): pdf_reader.py::_maybe_hybrid_ocr (стр.173, гейт кандидата+вызов hybrid_recover); ocr.py::is_hybrid_candidate (92), hybrid_recover (215), full_ocr/_lines_from_tsv (330-400, готовая машинерия полностраничного OCR+сборки строк), merge_line/_cyr_key (102-130, НЕ ослаблять); textnorm.looks_glyph_corrupted (готовый детектор латиница→кириллица); локализация списка — clinical.py::_NAMED_SECTIONS (abbreviations=«список сокращ», terms=«термины и определ», стр.77-81); атомарная запись — jsonio._atomic_dump; батч — batch_report.py ProcessPoolExecutor(14, spawn) → гонку по общему файлу исключить шардами. Факты КР1_4: сырой слой 207 «§»/30 «ВСЬС», в текущем выводе §=0 но ВСЬС=28 (строки без §) остаются; список сокращений битый, но с чистыми рус.пояснениями; Hepatitis B/C virus и авторы в списке отсутствуют → их закрывает полностраничный OCR.

---

```
Доведи OCR-восстановление битого текстового слоя КР до максимальной ПОЛНОТЫ по
согласованной схеме. Правишь ТОЛЬКО OCR-путь (crparser/engine/ocr.py и гейт в
pdf_reader.py) + новый модуль базы пинов. Структуру/таблицы/схему/метаданные/
валидатор НЕ трогаешь. Приоритет — КАЧЕСТВО (сканирование одноразовое), время вторично.

=== ПРОБЛЕМА (проверено: pdftotext -layout + outout\КР1_4.json) ===
Гибрид (ocr.py::hybrid_recover, стр.215) OCR-ит ТОЛЬКО строки, где есть токен с «§»
или кирилло-латинская мешанина (_is_broken_token). Битая латиница, отрендеренная в
ЧИСТУЮ кириллицу-двойник (ВСЬС=BCLC, Ыуег=Liver, Нерабб=Hepatitis, СЫШ/Ри§Ь=Child-Pugh,
АЬВ1=ALBI, У1гиз=virus, апб-НСУ=anti-HCV, Ю0=100) на строках БЕЗ «§»/мешанины —
пропускается. В КР1_4: сырой слой 207 «§» и 30 «ВСЬС»; в текущем выводе «§»=0 и BCLC=4
(§-строки, починены), но ВСЬС=28 остаются (строки без §). Закрыть полностью.

=== ЦЕЛЕВОЙ КОНВЕЙЕР (для документа-КАНДИДАТА) ===
1. КАНДИДАТ (широкий триггер) = §-плотность (текущий is_hybrid_candidate) ИЛИ
   псевдо-латинский кластер (textnorm.looks_glyph_corrupted по всему тексту — уже ловит
   латиница→кириллица: ВСЬС/Ыуег/Мапбагб) ИЛИ во тексте есть кривой якорь из общей базы
   пинов (п.4). Чистые файлы (нет признаков) — ноль работы, как сейчас.
2. Для кандидата — OCR ВСЕХ страниц (rus+eng), полнота важнее скорости. Затем
   КОНСЕРВАТИВНОЕ слияние merge_line на ВСЕХ строках (не только §-строках): нативная
   кириллица остаётся АВТОРИТЕТНОЙ базой (Tesseract по-русски ошибается — ё/е, переносы,
   пунктуация), OCR-латиница подставляется ТОЛЬКО туда, где нативный токен не совпал по
   кириллическому якорю (битый двойник). merge_line/_cyr_key НЕ ослаблять.
3. Словарь-коррекция из «Списка сокращений»: локализовать регион, OCR его ОДИН раз →
   чистые аббревиатуры, сопоставить ПО ПОЗИЦИИ с кривыми токенами слоя → карта
   {кривое→чистое} документа. Восстановленные в теле токены «примагничивать» к карте
   (чинит ошибки OCR BClC→BCLC, даёт консистентность всех вхождений).
4. ОБЩАЯ КРОСС-ДОК БАЗА ПИНОВ: персистентный JSON {кривое→чистое}, пополняется картами
   всех документов; используется как якоря-триггеры (п.1) и для пред-разрешения частых
   терминов без повторного OCR.
5. Собирать новые мэппинги (внесписочные термины/фамилии/шкалы) в базу.

=== ТОЧКИ ИНТЕГРАЦИИ В КОДЕ ===
- pdf_reader.py::_maybe_hybrid_ocr (стр.173) — расширить гейт кандидата (п.1); если
  кандидат — передать в hybrid_recover флаг «все строки» + карту коррекции. После —
  _recount_corruption (уже есть).
- ocr.py::is_hybrid_candidate (92) — добавить ветки looks_glyph_corrupted и «есть якорь
  из базы пинов». § остаётся быстрым выходом.
- ocr.py::hybrid_recover (215) — для кандидата OCR-ить ВСЕ страницы целиком и сливать
  КАЖДУЮ строку слоя с её OCR-строкой. Переиспользуй готовую машинерию full_ocr/
  _lines_from_tsv (330-400): рендер страницы + группировка TSV в строки с bbox; сопоставь
  каждой Line слоя OCR-строку по вертикальному перекрытию (y-band, как _words_to_bands,
  289), затем merge_line(line.text, ocr_line_text). Точечный fallback _recover_line
  оставить. Для НЕ-кандидатов путь не меняется.
- Словарь: регион по Line-якорям. Регэкспы есть — clinical.py::_NAMED_SECTIONS
  (abbreviations=«список\s+сокращ», terms=«термин\w*\s+и\s+определ», стр.77-81) и
  toc.py::_BODY_MARKERS (121). Взять страницы/bbox строк от якоря «Список сокращений» до
  следующего NAMED («Термины и определения») — регион для разового OCR. Разобрать записи
  «КЛЮЧ - определение»/«КЛЮЧ — определение»; чистый OCR даёт чистый КЛЮЧ (BCLC/AASLD/EASL),
  слой — кривой (ВСЬС/АА8ЬИ) → карта по позиции. Часть ключей уже чистая латиница
  (ECOG PS, LI-RADS) — не ломать. Термины вне списка (Hepatitis B/C virus, фамилии) —
  закрывает полностраничный OCR (п.2).
- База пинов: НОВЫЙ файл crparser/data/ocr_pins.json (каталог создать). Формат — плоский
  {"кривое":"чистое"}, UTF-8, ensure_ascii=false. Грузить ЛЕНИВО один раз; пополнять.

=== БЕЗ ГОНКИ ПРИ БАТЧЕ (batch_report.py: ProcessPoolExecutor, 14 воркеров, spawn) ===
НЕ делать read-modify-write общего ocr_pins.json из воркеров:
  * ocr_pins.json грузится ТОЛЬКО НА ЧТЕНИЕ (якоря + пред-разрешение);
  * карта документа пишется в отдельный шард crparser/data/pins/<id>.json АТОМАРНО
    (паттерн jsonio._atomic_dump: temp+fsync+os.replace);
  * слияние шардов в ocr_pins.json — отдельным single-process шагом в конце
    batch_report.main (после пула) ИЛИ ленивым объединением при загрузке.

=== КЛЮЧЕВОЕ ТРЕБОВАНИЕ КАЧЕСТВА ===
Нативная кириллица — БАЗА и авторитет; OCR подставляется только для битой латиницы/
двойников, не совпавших по кириллическому якорю. Чистый русский НЕ деградирует (ё/е,
переносы, пунктуация слоя сохраняются). merge_line не ослаблять.

=== ЯВНЫЕ ГРАНИЦЫ ===
- Чистые файлы (НЕ кандидаты) не трогаются: без рендера/Tesseract/замедления, JSON
  байт-в-байт прежний.
- Менять ТОЛЬКО ocr.py, гейт pdf_reader.py::_maybe_hybrid_ocr, новый модуль/каталог пинов
  и (для слияния шардов) хвост batch_report.main. НЕ трогать: segmenter, toc-арбитраж,
  детекторы таблиц, метаданные, схему JSON, validate.py, textnorm (только ИСПОЛЬЗОВАТЬ
  его детекторы, не править пороги).
- Дерево/таблицы/поля JSON не меняются — меняется только ТЕКСТ восстановленных токенов.
- Graceful fallback: нет Tesseract или rus — recoverer.available()=False, документ
  парсится как без OCR (тихо, без падения). Сохранить.
- БЕЗ хардкода строк/аббревиатур/имён файлов/фамилий. Правила структурные (§, детектор
  глифов, позиция в «КЛЮЧ - определение»).

=== ТЕСТЫ-ПИНЫ (ТОЛЬКО КР1_4 и КР628_2; НЕ весь корпус) ===
Пересобрать outout\КР1_4.json и outout\КР628_2.json, показать ДО/ПОСЛЕ по токенам.
КР1_4 (главный):
  * ВСЬС → 0; BCLC во ВСЕХ вхождениях (было 28 «ВСЬС» + 4 «BCLC»).
  * Hepatitis C virus, Child-Pugh (в разделах 2.3 и 3.1), ALBI + расшифровка
    Albumin-Bilirubin, anti-HCV, HBsAg, 100×10⁹ — во всех вхождениях (нет
    ВСЬС/Ри§Ь/НВзА§/У1гиз/апб-НСУ/АЬВ1/Ю0).
  * Чистая кириллица и структура целы: sections_found и дерево не изменились, coverage
    не упал (был 99.66), заголовки разделов те же.
  * validate.py: §-детектор = 0; статус REVIEW→PASS.
КР628_2: §-детектор = 0; кривые двойники в теле восстановлены; структура/coverage
  (был 99.73) целы; статус REVIEW→PASS (если REVIEW был только из-за §).
РЕГРЕССИЯ (обязательна): КР1000_1 — чистый PASS, §=0, НЕ кандидат → НЕ идёт через OCR →
  outout\КР1000_1.json байт-в-байт прежний.

=== РЕГЛАМЕНТ ===
1. Прогон ТОЛЬКО на КР1_4, КР628_2 (+ КР1000_1 регресс). Полный корпус НЕ гонять.
2. Показать таблицу ДО/ПОСЛЕ по токенам и статусам validate.py.
3. Коммит ТОЛЬКО после подтверждения пинов на 2 файлах И нулевого регресса на КР1000_1.
   Сообщение — деловое, по-русски, без упоминания Claude/AI и без Co-Authored-By.
   ocr_pins.json коммитить; шарды crparser/data/pins/ — в .gitignore (рабочий кэш).
```
