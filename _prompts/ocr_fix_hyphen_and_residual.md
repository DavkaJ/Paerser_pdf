# Фикс: латинизация русского в дефис-токенах (регрессия) + точечные недочистки

Тестировать ТОЛЬКО на КР1_4, КР628_2 (+КР1000_1 регресс). Использовать существующий кэш OCR. Весь корпус НЕ гонять.

## Что найдено в outout\КР1_4.json (полный скан)
Тело в целом восстановлено (BCLC, HBsAg, Child-Pugh, ECOG, RECIST/mRECIST, VEGFR, анти-CTLA, STRIDE, Himalaya — чисто). Остаток:
1. **РЕГРЕССИЯ (главное): `bridge-Tepanuu` (стр.160)** — оригинал «Bridge-терапия». Кривой дефис-токен `ВгЫ^е-терапия` заменён ЦЕЛИКОМ на чтение OCR, и русское «терапии» вышло латинскими двойниками `Tepanuu`. Русская часть НЕ должна была подменяться.
2. `НВз` (стр.174) — единичный HBs не восстановлен.
3. Глоссарий (стр.286): ключ BCLC чист, но ОПРЕДЕЛЕНИЕ кривое — `Вагсе1опа С1ш1с 1луег Сапсег (с1азз1йсайоп)` → должно быть `Barcelona Clinic Liver Cancer (classification)`.
4. Термины (стр.346): `РЭ-Ы` / `РБ-Ы` (PD-L1) не восстановлены, хотя в теле PD-L1 чист.
НЕ трогаем: кириллическое удвоение/пробелы (`преимупцество`, `с8мг`), список литературы (стр.324), приложение-шкала (стр.354).

---

```
Задача: починить регрессию латинизации русского в дефис-токенах и добить 3 точечные
недочистки в КР1_4. Правим ТОЛЬКО OCR-путь (crparser/engine/ocr.py) и пины
(crparser/data/ocr_pins.json). Ничего вне OCR не трогаем.

=== БАГ 1 (ГЛАВНЫЙ, регрессия): латинизация РУССКОГО в дефис-токене ===
Факт: outout\КР1_4.json стр.160 содержит «bridge-Tepanuu». В источнике — «Bridge-терапия»
(в слое кривой токен вида «ВгЫ^е-терапия»: кривая латиница + РУССКАЯ морфема через дефис).
Текущая дефис-логика заменяет токен ЦЕЛИКОМ на чтение OCR, а OCR прочитал русское «терапии»
латинскими двойниками → «Tepanuu». Русская часть подменяться НЕ должна.

ПРАВИЛЬНОЕ ПОВЕДЕНИЕ: в дефис-склеенном токене заменять ТОЛЬКО те компоненты, которые
являются кривым двойником (кир-норма компонента = ключ карты/пина ИЛИ компонент детектируется
как глиф-двойник). Компоненты, являющиеся НОРМАЛЬНЫМИ русскими словами/морфемами, брать
ДОСЛОВНО ИЗ НАТИВНОГО СЛОЯ и никогда не заменять чтением OCR.
Ожидаемо: «ВгЫ^е-терапия» → «bridge-терапия» (латиница только в первом компоненте,
«терапия» — кириллица из слоя).

ЖЁСТКИЙ ИНВАРИАНТ (добавить как защиту по всему merge/replace-пути):
НИКОГДА не выводить латинский токен/компонент там, где нативный слой содержит валидное
русское слово. Если нативный компонент — нормальная кириллица (не двойник по детектору и не
ключ карты), он остаётся кириллицей, что бы ни прочитал OCR. Это предохранитель против
латинизации чистого русского — проверить, что он срабатывает и в merge_line, и в
дефис-резолвере, и в force-replace по словарю.

=== БАГ 2: точечные недочистки ===
2a. `НВз` (стр.174 тела) → `HBs`. Добавить нормализованный вариант в пины (в т.ч. как
    отдельный ключ, не только «НВзА§»→HBsAg).
2b. Глоссарий (стр.286): восстановить не только КЛЮЧ, но и ОПРЕДЕЛЕНИЕ записи:
    «ВСЬС - Вагсе1опа С1ш1с 1луег Сапсег (с1азз1йсайоп)» → «BCLC - Barcelona Clinic Liver
    Cancer (classification)». Т.е. OCR+merge/словарь должны обрабатывать ВСЮ строку записи
    глоссария, а не только левую часть до дефиса.
2c. Термины (стр.346): применить словарь/пины и к разделу «Термины и определения» —
    `РЭ-Ы`/`РБ-Ы` → `PD-L1` (оба варианта уже должны быть в пинах после прошлого захода;
    убедиться, что force-replace доходит до этого раздела, а не только до тела).

=== ГРАНИЦЫ ===
- Только ocr.py + ocr_pins.json. Структуру/схему/валидатор/таблицы/сегментер НЕ трогать.
- НЕ трогать кириллическое удвоение/склейку («преимупцество», «с8мг») — другой класс.
- НЕ трогать список литературы (стр.324) и приложение-шкалу (стр.354) — по договорённости.
- Чистые файлы (не кандидаты) — JSON байт-в-байт прежний.
- merge_line не ослаблять; нативная кириллица авторитетна; рус. сокращения (ТАХЭ/УЗИ/КТ/МРТ)
  и русские слова не латинизировать.
- Без хардкода конкретных строк (правила структурные: дефис-компоненты + кир-норма + детектор
  двойника + карта пинов).

=== СКОРОСТЬ ===
- Использовать существующий дисковый кэш OCR (crparser/data/ocr_cache); DPI/препроцессинг не
  менять (иначе кэш инвалидируется).
- НЕ гонять весь корпус. Только КР1_4, КР628_2, КР1000_1 через мини-скрипт с таблицей ДО/ПОСЛЕ.

=== ТЕСТЫ-ПИНЫ ===
КР1_4:
  * стр.160: «bridge-терапия» (латиница ТОЛЬКО в «bridge», «терапия» — кириллица);
    0 вхождений «Tepanuu» и любых латинизированных русских слов.
  * 0 вхождений «НВз» (есть «HBs»).
  * Глоссарий: «BCLC - Barcelona Clinic Liver Cancer (classification)» — определение читаемо
    (нет «Вагсе1опа/1луег/с1азз1йсайоп»).
  * Термины: 0 вхождений «РЭ-Ы»/«РБ-Ы» (есть «PD-L1»).
  * Ранее восстановленное НЕ сломано: BCLC, HBsAg, Child-Pugh (не обрезан до «Child»), ECOG,
    RECIST/mRECIST, VEGFR, анти-CTLA4, STRIDE, Himalaya — остаются чистыми.
  * Кириллица цела; «преимупцество»/«с8мг» остаются как есть (их не трогаем).
КР628_2: латинизации русских слов НЕТ; ранее восстановленное не сломано.
РЕГРЕССИЯ: КР1000_1 (не кандидат) → JSON байт-в-байт прежний.

=== РЕГЛАМЕНТ ===
Коммит только после прохождения пинов на КР1_4 и КР628_2 И нуля регресса на КР1000_1.
Сообщение — по-русски, деловое, без упоминания Claude/AI и без Co-Authored-By.
```
