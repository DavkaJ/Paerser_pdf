# Уточнение OCR: добить ТЕЛО и ГЛОССАРИЙ (библиографию не трогаем)

Тестировать ТОЛЬКО на КР1_4 и КР628_2 (+ КР1000_1 регресс). НЕ гонять весь корпус.

Строим ПОВЕРХ уже сделанного (коммит da0dbc5): полностраничный OCR кандидата + merge + база пинов уже есть. Здесь — три точечных улучшения качества по телу и глоссарию.

## Что осталось (проверено на outout\КР1_4.json)
Тело в основном очистилось, но остаток сконцентрирован:
1. РАЗРОЗНЕННЫЕ ПРОПУСКИ В ТЕЛЕ: `СЫЫ`(Child), `Риф`(Pugh), `НВзА`(HBsAg) поймались НЕ во всех вхождениях (строки-секции 110/139/167/187) — OCR/merge пропустил отдельные инстансы.
2. ГЛОССАРИЙ (front-matter «Список сокращений» + «Термины и определения», сейчас в excluded-секции «Оглавление»): плотная КУРСИВНАЯ латынь распознаётся частично — `AASLD - American Аззосгабоп Гог бге 8Шбу оГ Глуег`, `ТNМ`, `СЬПЛ`(Child) не дочищены.
Библиографию (Список литературы, иностранные цитаты) и приложения-шкалы В ЭТОТ ЗАХОД НЕ ТРОГАЕМ — оставляем как known-issue.

---

```
Доведи OCR-восстановление по ТЕЛУ и ГЛОССАРИЮ КР (библиографию/references не трогаем).
Строишь ПОВЕРХ существующего OCR-пути (crparser/engine/ocr.py, гейт pdf_reader.py,
база пинов). Структуру/схему/валидатор/таблицы не меняешь. Приоритет — качество.

=== ТРИ УЛУЧШЕНИЯ ===

1. АГРЕССИВНАЯ КОРРЕКЦИЯ ПО СЛОВАРЮ (чинит разрозненные пропуски в теле + консистентность).
   После OCR+merge применить ФИНАЛЬНЫМ проходом карту {кривое→чистое} (из «Списка
   сокращений» документа + кросс-док база пинов ocr_pins.json) как ПОЛНУЮ ЗАМЕНУ по
   всему тексту секций и глоссария: каждое вхождение известного кривого токена →
   его чистая форма, независимо от того, попала ли строка в OCR. Так «СЫЫ/Риф/НВзА»,
   пропущенные OCR-ом на отдельных строках, дочистятся по словарю.
   - Замена по ГРАНИЦЕ ТОКЕНА (не подстрока), точное совпадение кривого ключа.
   - Только для документов-кандидатов; чистых не трогать.
   - Двунаправленно пополнять: успешно восстановленные OCR-ом токены (кривое→чистое)
     дописывать в карту документа и в базу пинов — чтобы их же вхождения в других
     местах дочистились и накопились на будущее.

2. ВЫШЕ DPI + ПРЕДОБРАБОТКА (чинит плотный курсивный глоссарий).
   Для OCR кандидата поднять DPI рендера с текущего (≈300) до 400 (константа, с
   комментом). Перед подачей в Tesseract — лёгкая предобработка растра: оттенки серого
   + апскейл ×1.5–2 при мелком кегле + (опц.) бинаризация/шарп. Особенно для региона
   глоссария (мелкий/курсивный шрифт). Через PIL (уже используется в _strip_ocr).
   psm: для строк глоссария «КЛЮЧ - определение» — как строки (--psm 7/6).

3. ПОКРЫТЬ ГЛОССАРИЙ OCR+MERGE.
   Убедиться, что регион «Список сокращений» и «Термины и определения» (front-matter,
   сейчас может лежать в excluded «Оглавление») реально проходит OCR+merge, а не
   остаётся сырым слоем. Локализация региона — по Line-якорям NAMED-разделов
   (clinical.py::_NAMED_SECTIONS: abbreviations/terms). OCR его при повышенном DPI,
   слить merge_line построчно, затем прогнать словарь-коррекцию (п.1).

=== ГРАНИЦЫ ===
- Трогать ТОЛЬКО OCR-путь (ocr.py, гейт pdf_reader.py, база пинов). Не трогать:
  структуру разделов/дерево/таблицы/схему/метаданные/validate.py; merge_line не
  ослаблять (кириллица-база авторитетна).
- Чистые файлы (не кандидаты) — без изменений (без OCR/замедления), JSON прежний.
- Библиографию (Список литературы) и приложения-шкалы в этот заход НЕ дочищаем —
  фокус на теле и глоссарии; не регрессировать их.
- Словарь-замена — по границе токена, точное совпадение; без хардкода конкретных
  строк/аббревиатур (карта строится из списка сокращений + базы пинов).
- Graceful fallback без Tesseract/rus сохранить.

=== ТЕСТЫ-ПИНЫ (ТОЛЬКО КР1_4, КР628_2; + КР1000_1 регресс) ===
КР1_4:
  * В ТЕЛЕ: 0 вхождений `СЫЫ`, `Риф`, `НВзА`, `Ри§`, `ВСЬС`, `У1гиз`, `апб`, `АЬВ1`;
    Child-Pugh / HBsAg / BCLC / anti-HCV / ALBI / Hepatitis C virus / 100×10⁹ — во
    ВСЕХ вхождениях тела.
  * В ГЛОССАРИИ: `AASLD`, `EASL`, `Association`, `Study`, `Liver`, `TNM`, `Child-Pugh`,
    `RECIST` — читаемы (не `Аззосгабоп/Глуег/ТNМ/СЬПЛ`).
  * Чистая кириллица и структура целы; coverage не упал (99.66); §-детектор
    validate.py = 0 (по телу+глоссарию); статус REVIEW→PASS (библиография может
    оставить остаточные §, если её не чистим — тогда отметить как known-issue, но по
    телу/глоссарию чисто).
КР628_2: тело и глоссарий аналогично очищены; структура/coverage целы.
РЕГРЕССИЯ: КР1000_1 (чистый, не кандидат) → outout\КР1000_1.json байт-в-байт прежний.

=== СКОРОСТЬ И КЭШ OCR (важно — прошлый прогон шёл ~2 часа) ===
1. КЭШ OCR на диск. OCR детерминирован для (id документа + номер страницы + DPI +
   langs + препроцессинг). Кэшировать сырой результат Tesseract (TSV/текст) в
   crparser/data/ocr_cache/<id>_<page>_<dpi>.json (каталог создать; в .gitignore).
   Перед вызовом Tesseract — проверять кэш; при попадании не рендерить и не звать
   Tesseract. Это радикально ускоряет повторные итерации на тех же файлах.
2. Ключ кэша включает версию препроцессинга/DPI, чтобы смена параметров инвалидировала
   кэш. Запись атомарная (temp+os.replace).

=== РЕГЛАМЕНТ ===
1. НЕ запускать batch_report по всему корпусу и НЕ пересобирать полный _corpus/report.json.
   Парсить ТОЛЬКО КР1_4, КР628_2, КР1000_1 — по одному файлу (CLI/минимальный скрипт) во
   временную папку; валидировать только их (validate.py на этих трёх). Полный прогон —
   отдельно и позже, по явному запросу.
2. Мини-харнес: маленький скрипт, который парсит эти 3 файла и печатает таблицу ДО/ПОСЛЕ
   по токенам тела и глоссария (СЫЫ/Риф/НВзА/ВСЬС/Ри§ vs Child-Pugh/HBsAg/BCLC/anti-HCV/
   Association/Liver/TNM) + статус validate.py. Гонять его, а не весь корпус.
3. Коммит только после подтверждения пинов и нуля регресса на КР1000_1. Сообщение —
   по-русски, деловое, без упоминания Claude/AI и без Co-Authored-By.
   crparser/data/ocr_cache/ — в .gitignore (рабочий кэш).
```
