# Разложить остаток корпуса по папкам (review / fail_ok / fail_broken)

Выполнять на живой X:\parser. PASS уже выгружены в outout_pass/. Здесь раскладываем НЕ-PASS. Все папки out*/ попадают под .gitignore — в git НЕ коммитить, это только организация файлов.

Классификация 56 FAIL сделана ревью-триажем по живым JSON (критерий: число канонических глав 1-7 узлами верхнего уровня; BROKEN — обвал структуры / потеря целой главы / нулевой парс).

```
Задача: разложить не-PASS выводы по папкам для удобства. Только копирование файлов и
печать счётчиков. Код/парсер/validate НЕ трогать. Ничего не коммитить (папки под out*/).

Источник статусов — X:\parser\_corpus\report.json (актуальный). Файлы брать из X:\parser\outout\.

1. outout_review/  ← скопировать все outout/<file>.json, где status == "REVIEW".
   (Ожидается ~49. Это порча текста — трек пере-OCR.)

2. outout_fail_broken/ ← скопировать outout/<file>.json для файлов из СПИСКА BROKEN:
   КР715_2, КР891_1, КР284_2, КР662_2, КР78_2, КР997_1,
   КР115_2, КР675_2, КР839_1, КР136_2, КР841_1, КР969_1, КР467_3,
   КР674_3, КР723_2, КР750_1
   (16 файлов: обвал глав / потеря целой главы / нулевой парс. Последние три — пограничные:
   главы формально есть, но потерян узел одной большой главы; помещены сюда консервативно.)

3. outout_fail_ok/ ← скопировать outout/<file>.json для ВСЕХ ОСТАЛЬНЫХ файлов со status=="FAIL",
   которых НЕТ в списке BROKEN выше.
   (Ожидается ~40: структура цела, MISSING = фантом/дефект источника/мелкий подраздел. Пригодны
   для корпуса с пометкой «structure ok, validator/source flag».)

4. Проверка: напечатать число файлов в каждой папке. Инвариант: |fail_broken| + |fail_ok| ==
   (число FAIL в report.json). Если не сходится — вывести расхождение (какие FAIL не попали никуда).

5. Сканы (SKIPPED_SCAN, ~21) в report.json отсутствуют (JSON не создавался). Их список взять из
   X:\parser\scan_candidates.csv. НЕ копировать JSON (его нет); просто напечатать список этих файлов
   как «трек OCR» (PDF в data/raw). Опционально: скопировать соответствующие PDF в scans_for_ocr/.

Ничего не коммитить. Вывести итоговую сводку: сколько в pass/review/fail_ok/fail_broken/scans.
```
