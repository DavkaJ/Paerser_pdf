# Продуктовые зависимости парсера КР
PyMuPDF>=1.24
pdfplumber>=0.11
openpyxl>=3.1
Pillow>=10
fontTools>=4.50
jsonschema>=4.0

# Морфология русского — ЯЗЫКОВОЙ АРБИТР канала C5 (гейт 2, crparser/engine/rumorph.py).
# Локально, офлайн, детерминированно. Единственное, что отличает русское слово от
# латинского, отрендеренного кириллицей: визуальным методом они НЕразличимы (I21/I22).
# ОТСУТСТВИЕ ПАКЕТА -> канал C5 ВЫКЛЮЧАЕТСЯ ЦЕЛИКОМ (fail-closed, I5), а не «латинизирует
# всё подряд». Т.е. без него корпус беднее на класс C5, но НЕ испорчен.
pymorphy3>=2.0
pymorphy3-dicts-ru>=2.4

# OCR-восстановление (опционально; при отсутствии Tesseract OCR тихо пропускается)
# — системный бинарь Tesseract + traineddata rus/eng, не pip-пакет.

# Разработка/тесты
pytest>=8.0
Flask>=3.0        # только для опционального web-интерфейса
