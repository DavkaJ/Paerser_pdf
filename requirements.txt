# Продуктовые зависимости парсера КР
PyMuPDF>=1.24
pdfplumber>=0.11
openpyxl>=3.1
Pillow>=10
fontTools>=4.50
jsonschema>=4.0

# OCR-восстановление (опционально; при отсутствии Tesseract OCR тихо пропускается)
# — системный бинарь Tesseract + traineddata rus/eng, не pip-пакет.

# Разработка/тесты
pytest>=8.0
Flask>=3.0        # только для опционального web-интерфейса
